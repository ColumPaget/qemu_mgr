#ifndef QEMU_MGR_IMAGE_DEFAULTS_H
#define QEMU_MGR_IMAGE_DEFAULTS_H

#include "common.h"

char *ConfigTemplatesGetList(char *RetStr);
char *ConfigTemplateLoad(char *RetStr, const char *Name);

#endif
